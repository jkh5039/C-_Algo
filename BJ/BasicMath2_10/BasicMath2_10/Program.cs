using System;

namespace BasicMath2_10
{
    class Program
    {
        static void Main(string[] args)
        {
            int R = int.Parse(Console.ReadLine());
            Console.WriteLine(Math.PI * R * R);
            Console.WriteLine(2 * R * R);
        }
    }
}
